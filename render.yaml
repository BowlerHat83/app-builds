services:
  - type: web
    name: seo-audit-backend
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PAGESPEED_API_KEY
        sync: false
      - key: SERPAPI_KEY
        sync: false
